def cek_angka(a, b, c):
    try:
        if a == b or a == c or b == c:
            return False
        elif a + b == c or a + c == b or b + c == a:
            return True
        else:
            return False
    except:
        ValueError
        return("masukkan integer")
